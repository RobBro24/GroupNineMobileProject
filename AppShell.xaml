<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="GroupNineMobileProject.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:GroupNineMobileProject"
    Shell.FlyoutBehavior="Flyout"
    Title="GroupNineMobileProject">

    <TabBar>
        <Tab Title="Home" Icon="homeicon.png">
            <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate local:HomePage}"
            Route="HomePage" />
        </Tab>

        <Tab Title="Games" Icon="gamesicon.png">
            <ShellContent
            Title="Games" 
            ContentTemplate="{DataTemplate local:GamesPage}"
            Route="GamesPage" />
        </Tab>

        <Tab Title="Profile" Icon="profileicon.png">
            <ShellContent
            Title="Profile" 
            ContentTemplate="{DataTemplate local:LoginPage}"
            Route="ProfilePage" />
        </Tab>

        <Tab Title="Settings" Icon="settingsicon.png">
            <ShellContent
            Title="Settings" 
            ContentTemplate="{DataTemplate local:SettingsPage}"
            Route="SettingsPage" />
        </Tab>
    </TabBar>

</Shell>
